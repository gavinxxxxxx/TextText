<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fitsSystemWindows="true"
        android:orientation="vertical">

        <include
            android:id="@+id/includeToolbar"
            layout="@layout/layout_toolbar" />

        <android.support.v7.widget.RecyclerView
            android:id="@+id/recycler"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:paddingBottom="8dp"
            android:paddingTop="8dp"
            app:fastScrollEnabled="false"
            app:fastScrollHorizontalThumbDrawable="@drawable/sn_thumb_scroll_bar"
            app:fastScrollHorizontalTrackDrawable="@drawable/bg_track_scroll_bar"
            app:fastScrollVerticalThumbDrawable="@drawable/sn_thumb_scroll_bar"
            app:fastScrollVerticalTrackDrawable="@drawable/bg_track_scroll_bar"
            app:layoutManager="LinearLayoutManager"
            tools:listitem="@layout/item_explorer" />

    </LinearLayout>

</layout>