<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ToolbarLightOverflow" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- title 颜色  -->
        <item name="android:textColorPrimary">@color/colorTitle</item>
        <!-- 菜单展开图标颜色 三点 -->
        <item name="android:textColorSecondary">@color/colorVector</item>
        <!-- 非展开巷 title 颜色 -->
        <item name="android:textColor">@color/textColorPrimary</item>
    </style>

    <style name="Toolbar.TitleOverlay" parent="TextAppearance.Widget.AppCompat.Toolbar.Title" />

    <style name="Toolbar.SubtitleOverlay" parent="TextAppearance.Widget.AppCompat.Toolbar.Subtitle">
        <item name="android:textSize">10sp</item>
    </style>

</resources>
